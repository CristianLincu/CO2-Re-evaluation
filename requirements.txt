# scikit-learn is pinned exactly: the saved models contain pickled estimators,
# which are not guaranteed to load across minor versions.
scikit-learn==1.5.2
xgboost>=3.0,<4
numpy>=2.0,<3
pandas>=2.2,<4
pyarrow>=15
joblib>=1.3
requests>=2.31
matplotlib>=3.8

scipy>=1.10,<2

